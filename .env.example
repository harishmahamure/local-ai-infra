# LAN bind and endpoints (this Mac)
REMOTE_HOST=gpu-box
LAN_BIND_IP=192.168.50.100
LLAMA_URL=http://192.168.50.100:8080/v1

# Control API (Mac proxy forwards to GPU)
CONTROL_URL=http://127.0.0.1:8090
GPU_CONTROL_URL=http://192.168.50.100:8090

# Hugging Face token for gated model downloads on GPU box
HF_TOKEN=

# Remote paths — Linux paths ON the GPU box. Leave unset unless you
# changed the layout there. Do not use ~/... here (it expands on this Mac).
# REMOTE_AI_DIR=/home/harishmahamure/ai-inference/control
# AI_VENV=/home/harishmahamure/ai-inference/venv
